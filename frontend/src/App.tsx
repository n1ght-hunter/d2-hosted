import { BrowserRouter } from 'react-router-dom';
import { CssBaseline } from '@mui/material';

const App = () => {
  return (
    <BrowserRouter>
      <CssBaseline />
    </BrowserRouter>
  );
};

export default App;
